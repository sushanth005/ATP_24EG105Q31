### Steps to create Backend
1. Generate package.json file
    npm init -y
    ### Automatically generates package.json file
2. Create HTTP Server
    - Install express module
        npm install express
        ### it'll download node js to our appliccation
    - Import express module
3.
### testing using REST client extension
### we create a req.http to send the requests as a client to the server

### install nodemon to get more information

### start the server with nodemon server.js

### using npx we can module without installing