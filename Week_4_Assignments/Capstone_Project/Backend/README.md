###
Generate package.json
init 
Create .env file
npm install dotenv
create express app and assign port number
connect to database
npm install mongoose
define schema's and create models
    -UserType Schema
        firstname
        lastanme
        email(unique)
        password
        role
        profile Image Url
        isUserActive
    -Article Schema
        author
        title
        category
        content
        comments
        isArticleActive

Implement API's
create common api for register, login and logout



### Frontend
    Dynamic, Responsive User Interfaces(UI = web page -> Browser)
                            HTML -> not a programming language, it is a markup language -> skeleton of the web page
                            CSS->cascading style sheet(styles and responsiveness) -> frameworks like Bootstrap, TailwindCSS
    JavaScript is needed to make the page dynamic updating new data
    ReactjS/Angular/Vue/NextJS
    
### 